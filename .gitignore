# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Elixir LSP files
.elixir_ls/

# Ignore compiled .beam files
*.beam

# Ignore package tarball (built via "mix hex.build").
rsolv_api-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# Ignore generated OpenAPI spec (regenerate with: mix rsolv.openapi)
/priv/static/openapi.json
openapi.json

# ugh, somehow we ended up with node modules in here
node_modules/

# Database files
*.db
*.db-*

# Environment files
.env
.env.*
!.env.example
.envrc

# Editor and OS files
.DS_Store
Thumbs.db
*~
\#*\#
.\#*
.projectile
TAGS
tags
.idea/
.vscode/
*.swp
*.swo

# Claude Code local settings
.claude/*.local.json

# Temporary files
*.tmp
*.bak.env.dev
.env.local
nodegoat-demo/
nodegoat-demo-*/
nodegoat-vulnerability-demo/
rsolv-demo-vulnerable/

# RSOLV phase data artifacts
.rsolv/
