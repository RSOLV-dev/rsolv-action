# This workflow is deprecated - tests are run via ci.yml workflow
# which uses run-tests.sh with proper Bun/vitest configuration
name: Sharded Tests (Deprecated)

on:
  push:
    branches: [ disabled ]  # Disabled - ci.yml handles testing
  pull_request:
    branches: [ disabled ]

jobs:
  deprecated:
    name: This workflow is deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "⚠️ This workflow is deprecated"
          echo "Tests are now run via the main CI workflow (.github/workflows/ci.yml)"
          echo "which uses run-tests.sh with Bun and vitest"